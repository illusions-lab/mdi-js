{
  "name": "mdi-js",
  "private": true,
  "version": "2.0.1",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter docs dev",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm run build && pnpm --filter docs build",
    "build": "node scripts/verify-publication-layouts.mjs && turbo run build",
    "test": "turbo run test",
    "test:coverage": "node nodejs/scripts/check-node-coverage.mjs",
    "typecheck": "turbo run typecheck",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm run build && changeset publish",
    "bump-spec-version": "node scripts/bump-spec-version.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@vitest/coverage-v8": "^2.1.0",
    "turbo": "^2.1.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
