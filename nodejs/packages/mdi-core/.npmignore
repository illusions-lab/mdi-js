# `dist/` is generated by wasm-pack and intentionally ignored by Git, but it
# is the complete published payload of this package.  Keeping a package-local
# npmignore prevents npm from inheriting the workspace-level `dist` exclusion.
node_modules
coverage
