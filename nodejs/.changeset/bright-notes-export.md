---
"@illusions-lab/mdi": patch
"@illusions-lab/mdi-cli": patch
---

Add the Rust-backed `note` text format and `mdi build --to note`, including
native note ruby, editor shortcuts, readable fallbacks, and `txt-all` support.
